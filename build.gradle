plugins {
	id 'base'
	id 'org.antora' version '1.0.0-alpha.3'
}

antora {
    version = '3.2.0-alpha.2'
	options = ['--clean', '--fetch', '--stacktrace']

	playbook = file('local-antora-playbook.yml')

	dependencies = [
			'@springio/asciidoctor-extensions' : '1.0.0-alpha.5'
	]
}
