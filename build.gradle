plugins {
	id 'base'
	id 'org.antora' version '1.0.0'
}

antora {
    version = '3.2.0-alpha.4'
	packages = ['@antora/collector-extension':'latest']
	options = ['--clean', '--fetch', '--stacktrace']
}
